version: '3'

tasks:

  dev:
    cmds:
      - cargo run -p bot-fleet

  build:
    cmds:
      - cargo build -p bot-fleet

  test:
    cmds:
      - cargo test -p bot-fleet

  lint:
    cmds:
      - cargo fmt

  start:
    cmds:
      - cargo run --release