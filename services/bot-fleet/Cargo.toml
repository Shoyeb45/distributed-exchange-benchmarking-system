[package]
name = "bot-fleet"
version = "0.1.0"
edition = "2024"

[dependencies]
config.workspace = true
tokio = { version = "1", features = ["full"] }
kafka-service = { path = "../../libs/kafka-service" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"