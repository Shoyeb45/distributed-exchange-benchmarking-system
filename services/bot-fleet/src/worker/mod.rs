
pub async fn process_message() {
    // open ws connection between this and the sandbox-engine
    // and then send the init
    // then create the bot fleets
    // send the messages using grpc service
    // also maintain the order book and matching engine for each 
    
}